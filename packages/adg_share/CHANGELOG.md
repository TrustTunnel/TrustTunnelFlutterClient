# [0.0.1]

- Add the initial local `adg_share` Flutter plugin.
- Add Dart models, request validation, MIME resolution, and result mapping.
- Add Android native sharing through `ACTION_SEND`, `ACTION_SEND_MULTIPLE`, and `FileProvider`.
- Add iOS native sharing through `UIActivityViewController` with iPad popover origin support.
- Add package tests and public package documentation.
